import React from "react";
import { InnerSpendTrackingComponentProps } from ".";

export class SpendTrackingGraph extends React.Component<InnerSpendTrackingComponentProps> {
    render(): React.ReactNode {
        return (
            <p>Spend Tracking Graph</p>
        )
    }
}